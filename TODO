[Client]:


[Server]:
- Помимо проверки имени, надо еще придумать как проверить серийный номер ЖД (при подклчении клиента)
- Действие, если придет от клиента пустой
- Нормальная запись данных в файл клиента
- Нет возможности выйти назад при просмотре спискa request и user
- Valgrind ругается

[Questions]

[HW]:
- Разобраться с наименованием таблицы (+)

- Пользовательские данные из json -> db3 и в одной DB таблица с данными, таблица с очередью команд (+)
    - при регистрации создание БД пользователя с колонками (+)
    - почему так сложно подключается библиотека SQliteCpp (+)
    - перевод из requests в  users не корректно (+)
    - заполнить колонки значениями при регистрации (+)
            - надо как-то разложить файл json на значения и записать их в БД (+)
            - переделать принимаемый параметр при insert с std::vector в  std::map -> std::pair (+)

- INSERT в users отправляет vector, а должен std::pair (+)
    - делать ли шаблонным insert ? (НЕТ) +

- Действия админа, если пользователь уже зарегистрирован
    - можно ли как-то еще проверить не пустая БД, кроме как SQLite::Statement ? (не актуально)
    - неправильно работает director.SetBuilder (не меняет builder-ов) (Изменил указатель на ссылку) (+)
    - действия при получении данных от клиента (уже зарегистрированного) при повторном подключении
    - придумать действия, если больше нет запросов к клиенту

- Структура меню в целом (мб билдер ???)

- Навигация по user ???????

- Ошибка при обращении к БД (надо узнать пуста она или нет)
